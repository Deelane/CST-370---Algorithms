hw1 - Reads two timestamps of two events from a user and displays the difference between the two timestamps.
hw2 - Reads the number of elements in a set, prints the elements of the set, and then prints all possible decimal numbers, corresponding binary numbers, and subsets one by one.
hw3 - Checks if an input string is a palindrome or not.
